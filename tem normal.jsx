function xuLy(arr, wAll, hAll, FileDesign, FileName, type) {
    for (var i = 0; i <= arr.length - 1; i++) { // loop làm file in
        { // tạo nhãn
            #include "label 1.jsx";
        }
    } // hết làm file
}

